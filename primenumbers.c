#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<time.h>

void prime(int n){if((n+1)%2==0){rereturn 1;}
else{return 0;}}
} 
int main()
{
/*float r, A, C;
float pi=3.14;
int n;
printf("Hello World");
printf("Enter the value of the Radius\n");
scanf("%f",&r);
A=(r*r)*pi;
C=2*r*pi;
printf("The Area:%f\n",A);
printf("The circumference is:%f\n",C);
*/

/*Calculating the factorial
printf("Enter the number\n");
scanf("%d",&n);
if(n==0 || n==1){return 1;}
else{
    for(int i=1; i<=n; i++)
        {F=F*1;}
    return F;
    }
 */ 
 /*int n;
printf("Enter the number\n");
scanf("%d",&n);
long long result = factorial(n);
 printf("The factorial is:%ld\n",&result);  
return 0;
*/


//Finding Factorial, trivial and non trivial factors, find if a number is prime or nonot



/*int n;
printf("Enter the Number");
scanf("%d",&n);

if(n%2==0){return 1;}
else{return 0;}
 */
}